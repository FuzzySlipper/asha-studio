{
  "name": "asha-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent-observable ASHA Studio frontend shell.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "test": "tsx --test test/**/*.test.ts",
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "smoke:static": "node scripts/static-smoke.mjs",
    "verify": "pnpm run check:boundaries && pnpm run test && pnpm run build && pnpm run smoke:static"
  },
  "dependencies": {
    "@asha/command-registry": "link:../asha/ts/packages/command-registry",
    "@asha/contracts": "link:../asha/ts/packages/contracts",
    "@asha/editor-tools": "link:../asha/ts/packages/editor-tools"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  },
  "ashaStudio": {
    "task": 2735,
    "allowedAshaPackages": [
      "@asha/command-registry",
      "@asha/contracts",
      "@asha/editor-tools"
    ],
    "deferredPublicPackages": [
      "@asha/studio-evidence",
      "@asha/runtime-bridge",
      "@asha/devtools",
      "@asha/renderer-three"
    ],
    "knownLimitations": [
      "Task 2735 provides software_snapshot visual evidence and a fail-closed review_export artifact over the typed voxel workflow; native runtime bridge/browser capture remains deferred.",
      "@asha/studio-evidence is represented as a deferred public package until its implementation lands; this repo does not invent local artifact schemas."
    ]
  }
}
