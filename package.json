{
  "name": "asha-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent-observable ASHA Studio frontend shell.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "test": "tsx --test test/**/*.test.ts",
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "smoke:static": "node scripts/static-smoke.mjs",
    "proof:v1": "pnpm run verify && tsx scripts/v1-proof.ts && node scripts/readback-v1-proof.mjs",
    "proof:browser": "pnpm run proof:v1 && tsx scripts/browser-visual-capture.ts && node scripts/readback-browser-capture.mjs",
    "verify": "pnpm run check:boundaries && pnpm run test && pnpm run build && pnpm run smoke:static"
  },
  "dependencies": {
    "@asha/command-registry": "link:../asha/ts/packages/command-registry",
    "@asha/contracts": "link:../asha/ts/packages/contracts",
    "@asha/editor-tools": "link:../asha/ts/packages/editor-tools"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  },
  "ashaStudio": {
    "task": 2737,
    "allowedAshaPackages": [
      "@asha/command-registry",
      "@asha/contracts",
      "@asha/editor-tools"
    ],
    "deferredPublicPackages": [
      "@asha/studio-evidence",
      "@asha/runtime-bridge",
      "@asha/devtools",
      "@asha/renderer-three"
    ],
    "knownLimitations": [
      "Task 2737 provides Studio-owned command batch transaction metadata, per-command batch results, partial-failure classification, and a typed V1 voxel inverse setVoxel revert workflow over public command evidence.",
      "Task 2736 provides a public-contract model/material preview artifact using @asha/contracts StaticMeshAsset/MaterialProjection/RenderFrameDiff DTOs and records missing first-class command-registry/runtime preview surfaces as explicit follow-up work.",
      "Task 2738 provides `pnpm run proof:v1` as the end-to-end Studio V1 proof command with software_snapshot functional visual evidence.",
      "Task 2739 provides `pnpm run proof:browser` as a Chromium headless browser screenshot capture/readback path for the Studio app and V1 proof routes; it does not claim Agora/hardware GPU/performance evidence.",
      "@asha/studio-evidence remains deferred; current proof uses Studio-owned review/proof artifact schemas as reference evidence until that package lands."
    ]
  }
}
