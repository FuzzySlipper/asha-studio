# Proof-apparatus disposition

This is the terminal inventory for task #5860.

| Former artifact family | Disposition | Product replacement |
| --- | --- | --- |
| `scripts/proof-*.ts`, evidence dispatcher, and evidence catalog | Deleted | Focused local regressions and the small native product launcher |
| committed `artifacts/**`, visual contracts, and computed proof fixtures | Deleted | Ignored failure artifacts may be captured during a live run |
| milestone proof and visual-contract documentation | Deleted | Current workflows are documented in `editor-workflows.md` |
| scene-persistence cleanup plan and historical proof-script index | Deleted | Current persistence behavior is documented in `project-workspace-persistence.md` |
| allowlisted historical script references | Deleted | Documentation may name only runnable package commands |
| native voxel proof/interactive hybrid | Converted | `scripts/studio-native-host.ts` only builds and serves the usable editor |
| Publish Evidence and Evidence bottom-panel tabs | Deleted | Timeline, Assets, Catalog, and Commands retain operator workflows |
| renderer missing-resource probe button and synthetic channel mutation | Deleted | Renderer failures surface naturally through viewport diagnostics during real editing |
| cross-repository backend proof-ref gate | Deleted | Native attachment validates the public provider contract and Rust authority directly |
| source-token, fixture-refresh, and proof-process assertions in the scaffold test | Deleted | Local tests assert mutations, rejection, persistence, call counts, and UI outcomes |
| `globalThis.ashaStudioVoxelWorkflow` and transcript replay surface | Deleted | Trusted agents use the same visible menus, dialogs, and editor controls as people |
| Voxel Tools JSON automation tab | Deleted | Voxel Tools retains the human-facing Convert, Edit, Asset, Metadata, and History workflows |
| synthetic testing-fixtures library, conversion phase goldens, and demo asset pack | Deleted | Behavioral tests create only the inputs needed by the Studio-owned defect under test |
| hard-coded `asha-demo` bootstrap, fake command rows, and demo inventory | Deleted | Studio opens an empty untitled document and gives it a distinct Rust workspace-authoring cell |
| compatibility-evidence and compact-agent-readout surfaces | Deleted | Public provider attachment and visible editor read models expose actual current behavior |
| source-shaped renderer and voxel-conversion tests | Deleted | Product regressions exercise accepted and stale mutations plus visible native editing |
| host-file, renderer-host, transform settlement, scene/voxel persistence, lighting, and voxel authoring tests | Retained | Concrete Studio-owned defect regressions |

Useful public provider mutation/replay coverage was run from `asha-testing`
before the Studio copies were removed. No artifact was moved merely to preserve
historical proof machinery.
