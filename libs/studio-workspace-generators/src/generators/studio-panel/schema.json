{
  "$schema": "http://json-schema.org/schema",
  "title": "ASHA Studio Panel",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Panel name in file-name form, for example entity-browser."
    },
    "visualId": {
      "type": "string",
      "description": "Stable data-visual-id marker for browser/proof readback."
    }
  },
  "required": ["name", "visualId"]
}
